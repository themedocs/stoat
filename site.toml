title       = "Stoat docs"
# Origin only — page URLs already carry the path prefix.
base_url    = "https://docs.owldraft.com"
path_prefix = "/stoat/"
home_url    = "https://docs.owldraft.com/"
mark        = "owldraft-mark.jpg"

# Google Analytics 4 — one property covers every path under docs.owldraft.com.
ga_id       = "G-VGC71S6DLL"
